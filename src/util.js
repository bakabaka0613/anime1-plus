// 純工具函式（不依賴 GM / DOM，可被 node:test 直接 import）。

// 常用繁→簡對照（anime1 多為繁體、Bangumi 索引多為簡體，搜尋/比對前統一）。
// 涵蓋動畫標題高頻差異字；未涵蓋者保持原字（搜尋仍會同時試原文）。
const T2S = {
  處: '处', 劇: '剧', 戰: '战', 靈: '灵', 夢: '梦', 愛: '爱', 歲: '岁', 殺: '杀', 與: '与', 屬: '属',
  為: '为', 學: '学', 國: '国', 會: '会', 後: '后', 來: '来', 時: '时', 樂: '乐', 過: '过', 這: '这',
  還: '还', 進: '进', 運: '运', 動: '动', 萬: '万', 樣: '样', 義: '义', 經: '经', 網: '网', 線: '线',
  麼: '么', 們: '们', 個: '个', 風: '风', 雲: '云', 龍: '龙', 鳳: '凤', 寶: '宝', 劍: '剑', 鬥: '斗',
  戀: '恋', 觀: '观', 覺: '觉', 開: '开', 關: '关', 險: '险', 陽: '阳', 陰: '阴', 隊: '队', 雙: '双',
  變: '变', 邊: '边', 鄉: '乡', 錄: '录', 鋼: '钢', 錯: '错', 鏡: '镜', 長: '长', 門: '门', 間: '间',
  際: '际', 雜: '杂', 雞: '鸡', 離: '离', 難: '难', 飛: '飞', 餘: '余', 體: '体', 髮: '发', 鳥: '鸟',
  麗: '丽', 點: '点', 黨: '党', 齊: '齐', 齒: '齿', 號: '号', 電: '电', 話: '话', 語: '语', 說: '说',
  讀: '读', 誰: '谁', 請: '请', 謝: '谢', 認: '认', 識: '识', 證: '证', 議: '议', 護: '护', 讓: '让',
  響: '响', 頭: '头', 願: '愿', 顯: '显', 類: '类', 顧: '顾', 書: '书', 畫: '画', 圖: '图', 團: '团',
  園: '园', 區: '区', 醫: '医', 華: '华', 蘭: '兰', 萊: '莱', 葉: '叶', 蟲: '虫', 衛: '卫', 術: '术',
  製: '制', 複: '复', 裝: '装', 見: '见', 親: '亲', 覽: '览', 計: '计', 記: '记', 設: '设', 詞: '词',
  試: '试', 詩: '诗', 談: '谈', 謀: '谋', 講: '讲', 謎: '谜', 譯: '译', 貓: '猫', 財: '财',
  貨: '货', 責: '责', 貴: '贵', 買: '买', 費: '费', 賣: '卖', 賢: '贤', 質: '质', 贏: '赢', 軍: '军',
  軟: '软', 載: '载', 輕: '轻', 輪: '轮', 輸: '输', 轉: '转', 農: '农', 違: '违', 連: '连', 週: '周',
  遊: '游', 達: '达', 遠: '远', 適: '适', 選: '选', 遺: '遗', 錢: '钱', 錦: '锦', 鍵: '键', 鎮: '镇',
  鐘: '钟', 鐵: '铁', 階: '阶', 隨: '随', 隱: '隐', 雖: '虽', 霧: '雾', 靜: '静', 韓: '韩', 頁: '页',
  順: '顺', 預: '预', 領: '领', 頻: '频', 題: '题', 顏: '颜', 養: '养', 騎: '骑', 驚: '惊', 驗: '验',
  鬆: '松', 魚: '鱼', 鳴: '鸣', 麵: '面', 戲: '戏', 將: '将', 緣: '缘', 殤: '殇',
  徵: '征', 懲: '惩', 紀: '纪', 緋: '绯', 紅: '红', 約: '约', 級: '级', 紛: '纷', 純: '纯',
  紙: '纸', 細: '细', 終: '终', 組: '组', 結: '结', 絕: '绝', 給: '给', 統: '统', 絲: '丝', 綠: '绿',
  維: '维', 緊: '紧', 縣: '县', 縱: '纵', 總: '总', 績: '绩', 織: '织', 繪: '绘',
  繼: '继', 續: '续', 纏: '缠', 蓮: '莲',
  獻: '献', 聲: '声', 舊: '旧', 藝: '艺', 寫: '写', 傳: '传', 價: '价', 嚴: '严', 實: '实', 對: '对',
  發: '发', 機: '机', 權: '权', 歡: '欢', 熱: '热', 獨: '独', 獸: '兽', 療: '疗', 禮: '礼', 聖: '圣',
  聞: '闻', 興: '兴', 舉: '举', 藍: '蓝', 藥: '药', 豐: '丰', 車: '车', 辦: '办', 銀: '银', 陳: '陈',
  馬: '马', 廣: '广', 廳: '厅', 數: '数', 樓: '楼', 標: '标', 歸: '归', 滿: '满', 漢: '汉', 滅: '灭',
  潔: '洁', 燈: '灯', 營: '营', 環: '环', 現: '现', 產: '产', 畢: '毕', 當: '当', 盡: '尽', 監: '监',
  盤: '盘', 種: '种', 積: '积', 稱: '称', 競: '竞', 筆: '笔', 籠: '笼', 紹: '绍', 編: '编', 縮: '缩',
  繞: '绕', 罰: '罚', 聯: '联', 腦: '脑', 膽: '胆', 莊: '庄', 蓋: '盖', 螢: '萤', 裡: '里', 誕: '诞',
  誠: '诚', 諾: '诺', 賤: '贱', 賺: '赚', 較: '较', 醜: '丑', 鈴: '铃', 韻: '韵', 頓: '顿', 飼: '饲',
  駿: '骏', 鯨: '鲸', 鹽: '盐', 麥: '麦', 黃: '黄', 蝦: '虾', 嬌: '娇', 媽: '妈', 孫: '孙', 寧: '宁',
  寬: '宽', 尋: '寻', 導: '导', 帶: '带', 彈: '弹', 懷: '怀', 損: '损', 據: '据', 擊: '击', 擔: '担',
  敗: '败', 斷: '断', 棄: '弃', 檢: '检', 殘: '残', 氣: '气', 烏: '乌', 獎: '奖', 瘋: '疯', 盜: '盗',
  礙: '碍', 穩: '稳', 聽: '听', 臉: '脸', 艦: '舰', 蘇: '苏', 補: '补', 觸: '触', 訴: '诉', 詳: '详',
  諷: '讽', 贈: '赠', 輩: '辈', 辭: '辞', 遞: '递', 釣: '钓', 鋒: '锋', 鍾: '钟', 閃: '闪', 陸: '陆',
  頰: '颊', 騙: '骗', 驢: '驴', 鬧: '闹', 鮮: '鲜', 鴻: '鸿', 齡: '龄', 龜: '龟', 專: '专', 雋: '隽',
  諜: '谍', 諧: '谐', 誼: '谊', 謊: '谎', 緻: '致', 嘰: '叽',
};
export function toSimplified(s) {
  return Array.from(String(s || '')).map((c) => T2S[c] || c).join('');
}

// 全形英數 → 半形，方便名稱正規化比對
function toHalfWidth(s) {
  return s.replace(/[！-～]/g, (c) => String.fromCharCode(c.charCodeAt(0) - 0xfee0)).replace(/　/g, ' ');
}

// 名稱正規化：去空白、標點、轉小寫、全形轉半形。用於相似度比對。
export function normalizeName(s) {
  // 注意：不在此做繁簡統一——會讓短名被不相關長名「包含」而虛高、造成誤採。
  // 繁簡只用於搜尋關鍵字（searchAnime）。
  return toHalfWidth(String(s || ''))
    .toLowerCase()
    .replace(/[\s]/g, '')
    .replace(/[!?。．・:~\-—_、,「」『』()\[\]{}"'’“”…★☆※／/]/g, '');
}

// Levenshtein 編輯距離
export function levenshtein(a, b) {
  if (a === b) return 0;
  if (!a.length) return b.length;
  if (!b.length) return a.length;
  let prev = Array.from({ length: b.length + 1 }, (_, i) => i);
  for (let i = 1; i <= a.length; i++) {
    const cur = [i];
    for (let j = 1; j <= b.length; j++) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      cur[j] = Math.min(prev[j] + 1, cur[j - 1] + 1, prev[j - 1] + cost);
    }
    prev = cur;
  }
  return prev[b.length];
}

// 名稱相似度 0~1（正規化後）。一方包含另一方時給高分。
export function similarity(a, b) {
  const na = normalizeName(a);
  const nb = normalizeName(b);
  if (!na || !nb) return 0;
  if (na === nb) return 1;
  if (na.includes(nb) || nb.includes(na)) {
    const ratio = Math.min(na.length, nb.length) / Math.max(na.length, nb.length);
    return 0.8 + 0.2 * ratio;
  }
  const dist = levenshtein(na, nb);
  return 1 - dist / Math.max(na.length, nb.length);
}

// 節流：每 wait 毫秒最多執行一次（首呼立即、尾呼補一次）。
export function throttle(fn, wait) {
  let last = 0;
  let timer = null;
  let lastArgs = null;
  return function throttled(...args) {
    lastArgs = args;
    const now = Date.now();
    const remaining = wait - (now - last);
    if (remaining <= 0) {
      if (timer) { clearTimeout(timer); timer = null; }
      last = now;
      fn.apply(this, lastArgs);
    } else if (!timer) {
      timer = setTimeout(() => {
        last = Date.now();
        timer = null;
        fn.apply(this, lastArgs);
      }, remaining);
    }
  };
}

// 秒 → MM:SS 或 H:MM:SS
export function formatTime(sec) {
  if (!Number.isFinite(sec) || sec < 0) sec = 0;
  const s = Math.floor(sec % 60);
  const m = Math.floor((sec / 60) % 60);
  const h = Math.floor(sec / 3600);
  const pad = (n) => String(n).padStart(2, '0');
  return h > 0 ? `${h}:${pad(m)}:${pad(s)}` : `${m}:${pad(s)}`;
}
